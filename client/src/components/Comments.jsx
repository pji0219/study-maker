import React from 'react';
import styled from '@emotion/styled';

const Base = styled.ul`
  margin: 0 auto;
  width: 1200px;
  height: 100%;
  border: 1px solid #eeeeee;
`;

function Comments({ id }) {
  return <Base>comments</Base>;
}

export default Comments;
